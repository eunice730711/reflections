What happens when you initialize a repository? Why do you need to do it?

	create a .git directory in a repository. 
	.git directory stores the history of the git repository.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	staging area is the temporary area to store the files, if we add the wrong
	file, we can easily undo it.

How can you use the staging area to make sure you have one commit per logical
change?

	using the git diff to compare the working directory and staging area

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
	
	when you want to create a new version or add some experimental features.

How do the diagrams help you visualize the branch structure?

	to realize when I checkout a commit, what commit will loose.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
	
	to realize what to merge

What are the pros and cons of Git�䏭 automatic merging vs. always doing merges
manually?
	
	doing merges manually let us decide which version to choose

